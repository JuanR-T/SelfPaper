import Contact from '../ui/Contact';
import Nav from '../ui/Nav';

const Header = () => {
    return (
        <div className="header">
            <Nav />
            <Contact />
        </div>
    );
};

export default Header;
